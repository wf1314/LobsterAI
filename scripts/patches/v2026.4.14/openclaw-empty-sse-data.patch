diff --git a/src/agents/openai-transport-stream.ts b/src/agents/openai-transport-stream.ts
index b1c99aec..ddfd70c1 100644
--- a/src/agents/openai-transport-stream.ts
+++ b/src/agents/openai-transport-stream.ts
@@ -44,6 +44,12 @@ import { transformTransportMessages } from "./transport-message-transform.js";
 import { mergeTransportMetadata, sanitizeTransportPayloadText } from "./transport-stream-shared.js";
 
 const DEFAULT_AZURE_OPENAI_API_VERSION = "2024-12-01-preview";
+const OPENAI_COMPAT_EVENT_STREAM_CONTENT_TYPE = "text/event-stream";
+const OPENAI_COMPAT_EMPTY_SSE_DATA_PREFIX = "data:";
+const OPENAI_COMPAT_EMPTY_SSE_DATA_FRAME_LIMIT = 120;
+const OPENAI_COMPAT_SSE_FRAME_BOUNDARIES = ["\r\n\r\n", "\n\n", "\r\r"] as const;
+const OPENAI_COMPAT_EMPTY_SSE_ERROR =
+  "Provider stream emitted too many empty SSE data frames.";
 
 type BaseStreamOptions = {
   temperature?: number;
@@ -104,6 +110,140 @@ type MutableAssistantOutput = {
 
 export { sanitizeTransportPayloadText } from "./transport-stream-shared.js";
 
+type SseFrameBoundary = {
+  index: number;
+  length: number;
+};
+
+type EmptySseFrameFilterState = {
+  consecutiveEmptyFrames: number;
+  logged: boolean;
+};
+
+function isOpenAICompatEventStreamResponse(response: Response): boolean {
+  return (
+    response.headers
+      .get("content-type")
+      ?.toLowerCase()
+      .includes(OPENAI_COMPAT_EVENT_STREAM_CONTENT_TYPE) === true
+  );
+}
+
+function findSseFrameBoundary(buffer: string): SseFrameBoundary | null {
+  let result: SseFrameBoundary | null = null;
+  for (const boundary of OPENAI_COMPAT_SSE_FRAME_BOUNDARIES) {
+    const index = buffer.indexOf(boundary);
+    if (index < 0) {
+      continue;
+    }
+    if (!result || index < result.index) {
+      result = { index, length: boundary.length };
+    }
+  }
+  return result;
+}
+
+function shouldSkipEmptySseDataFrame(frame: string): boolean {
+  const dataLines = frame
+    .split(/\r\n|\n|\r/)
+    .filter((line) => line.startsWith(OPENAI_COMPAT_EMPTY_SSE_DATA_PREFIX));
+  return (
+    dataLines.length > 0 &&
+    dataLines.every(
+      (line) => line.slice(OPENAI_COMPAT_EMPTY_SSE_DATA_PREFIX.length).trim().length === 0,
+    )
+  );
+}
+
+function copyResponseWithBody(response: Response, body: ReadableStream<Uint8Array>): Response {
+  return new Response(body, {
+    status: response.status,
+    statusText: response.statusText,
+    headers: response.headers,
+  });
+}
+
+function emitFilteredSseFrames(
+  buffer: string,
+  controller: TransformStreamDefaultController<Uint8Array>,
+  encoder: TextEncoder,
+  state: EmptySseFrameFilterState,
+  model: Model<Api>,
+): string {
+  let remaining = buffer;
+  while (true) {
+    const boundary = findSseFrameBoundary(remaining);
+    if (!boundary) {
+      return remaining;
+    }
+
+    const frame = remaining.slice(0, boundary.index);
+    const delimiter = remaining.slice(boundary.index, boundary.index + boundary.length);
+    remaining = remaining.slice(boundary.index + boundary.length);
+
+    if (shouldSkipEmptySseDataFrame(frame)) {
+      state.consecutiveEmptyFrames += 1;
+      if (state.consecutiveEmptyFrames > OPENAI_COMPAT_EMPTY_SSE_DATA_FRAME_LIMIT) {
+        controller.error(new Error(OPENAI_COMPAT_EMPTY_SSE_ERROR));
+        return "";
+      }
+      if (!state.logged) {
+        state.logged = true;
+        console.debug(
+          `[OpenAICompatibleStream] skipped empty SSE data frames from ${model.provider}/${model.id}.`,
+        );
+      }
+      continue;
+    }
+
+    state.consecutiveEmptyFrames = 0;
+    controller.enqueue(encoder.encode(frame + delimiter));
+  }
+}
+
+function filterEmptySseDataFramesFromResponse(response: Response, model: Model<Api>): Response {
+  if (!response.body || !isOpenAICompatEventStreamResponse(response)) {
+    return response;
+  }
+
+  const decoder = new TextDecoder();
+  const encoder = new TextEncoder();
+  const state: EmptySseFrameFilterState = {
+    consecutiveEmptyFrames: 0,
+    logged: false,
+  };
+  let buffer = "";
+
+  const body = response.body.pipeThrough(
+    new TransformStream<Uint8Array, Uint8Array>({
+      transform(chunk, controller) {
+        buffer += decoder.decode(chunk, { stream: true });
+        buffer = emitFilteredSseFrames(buffer, controller, encoder, state, model);
+      },
+      flush(controller) {
+        const remainder = decoder.decode();
+        if (remainder) {
+          buffer += remainder;
+        }
+        buffer = emitFilteredSseFrames(buffer, controller, encoder, state, model);
+        if (buffer) {
+          controller.enqueue(encoder.encode(buffer));
+        }
+      },
+    }),
+  );
+
+  return copyResponseWithBody(response, body);
+}
+
+function buildOpenAICompletionsFetch(model: Model<Api>): typeof fetch {
+  const guardedFetch = buildGuardedModelFetch(model);
+  return async (input, init) => {
+    const response = await guardedFetch(input, init);
+    return filterEmptySseDataFramesFromResponse(response, model);
+  };
+}
+
 function stringifyUnknown(value: unknown, fallback = ""): string {
   if (typeof value === "string") {
     return value;
@@ -950,7 +1090,7 @@ function createOpenAICompletionsClient(
     baseURL: model.baseUrl,
     dangerouslyAllowBrowser: true,
     defaultHeaders: buildOpenAIClientHeaders(model, context, optionHeaders),
-    fetch: buildGuardedModelFetch(model),
+    fetch: buildOpenAICompletionsFetch(model),
   });
 }
 
@@ -1396,5 +1536,6 @@ function mapStopReason(reason: string | null) {
 }
 
 export const __testing = {
+  filterEmptySseDataFramesFromResponse,
   processOpenAICompletionsStream,
 };
diff --git a/src/agents/openai-transport-stream.test.ts b/src/agents/openai-transport-stream.test.ts
index 30479b5a..94fd2278 100644
--- a/src/agents/openai-transport-stream.test.ts
+++ b/src/agents/openai-transport-stream.test.ts
@@ -19,6 +19,53 @@ import {
 import { SYSTEM_PROMPT_CACHE_BOUNDARY } from "./system-prompt-cache-boundary.js";
 
 describe("openai transport stream", () => {
+  const openAICompletionsModel = {
+    id: "deepseek-v4-pro",
+    name: "DeepSeek V4 Pro",
+    api: "openai-completions",
+    provider: "custom_0",
+    baseUrl: "https://api.example.test/v1",
+    input: ["text"],
+    cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
+    contextWindow: 128000,
+    maxTokens: 8192,
+  } as unknown as Model<"openai-completions">;
+
+  it("drops empty SSE data frames before the OpenAI SDK stream parser sees them", async () => {
+    const response = new Response(
+      [
+        "data:\n\n",
+        'data: {"id":"chatcmpl-test","choices":[]}\n\n',
+        "data: [DONE]\n\n",
+      ].join(""),
+      { headers: { "content-type": "text/event-stream" } },
+    );
+
+    const filtered = __testing.filterEmptySseDataFramesFromResponse(
+      response,
+      openAICompletionsModel,
+    );
+
+    await expect(filtered.text()).resolves.toBe(
+      ['data: {"id":"chatcmpl-test","choices":[]}\n\n', "data: [DONE]\n\n"].join(""),
+    );
+  });
+
+  it("fails streams that emit too many consecutive empty SSE data frames", async () => {
+    const response = new Response("data:\n\n".repeat(121), {
+      headers: { "content-type": "text/event-stream" },
+    });
+
+    const filtered = __testing.filterEmptySseDataFramesFromResponse(
+      response,
+      openAICompletionsModel,
+    );
+
+    await expect(filtered.text()).rejects.toThrow(
+      "Provider stream emitted too many empty SSE data frames.",
+    );
+  });
+
   it("reports the supported transport-aware APIs", () => {
     expect(isTransportAwareApiSupported("openai-responses")).toBe(true);
     expect(isTransportAwareApiSupported("openai-codex-responses")).toBe(true);
